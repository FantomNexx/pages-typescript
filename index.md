## Install typescript
Run a command:
```npm i typescript -g```

## Compile ts to js
Run a command:
```tsc filename.js```

## Init project
Run a command:
```tsc --init```
It will make tsconfig.json with new project configuration.
